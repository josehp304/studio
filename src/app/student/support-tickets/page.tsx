
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function SupportTicketsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Support Tickets</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Create and track your support requests here.</p>
      </CardContent>
    </Card>
  );
}
